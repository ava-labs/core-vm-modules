# Core Module

## Overview

The core-module package provides common utilities that are reusable across different VM (Virtual Machine) modules. It includes essential types, functionalities, and logic to ensure consistency and reduce redundancy.

## Installation

The module is privately available on NPM as `@avalabs/core-module`
