---
'@avalabs/hypercore-module': minor
---

Add `withdrawable` to clearinghouse schema, `getWithdrawableUsd`, and `HypercoreInfoClient.fetchWithdrawableState` for Hyperliquid withdrawable balance (Fusion / Markr swap parity)
